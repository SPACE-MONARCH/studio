/**
 * @file Firestore Security Rules for Deadlock Avoidance Scenarios.
 *
 * Core Philosophy:
 * This ruleset allows public read access to all scenarios, making them available for learning purposes. Write access is completely disallowed, preventing unauthorized modifications.
 *
 * Data Structure:
 * All scenario data is stored in the top-level `/scenarios/{scenarioId}` collection.
 *
 * Key Security Decisions:
 * - Public read access is granted to the `/scenarios` collection to allow all users to access learning materials.
 * - All write operations (create, update, delete) are strictly denied on the `/scenarios` collection to prevent unauthorized data modification.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows public read access to scenario data. Denies all writes.
     * @path /scenarios/{scenarioId}
     * @allow (get, list): Any user can read scenario data.
     * @deny (create, update, delete): No user can create, update, or delete scenario data.
     * @principle Allows public read access while preventing unauthorized data modifications.
     */
    match /scenarios/{scenarioId} {
      allow get: if true;
      allow list: if true;

      allow create: if false;
      allow update: if false;
      allow delete: if false;
    }
  }
}